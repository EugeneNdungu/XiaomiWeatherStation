/**
 * Configuration Header File
 * 
 */

#ifndef CONFIG_H
#define CONFIG_H

#define BLE_ENABLE 1 // If enabled(set to > 0), all ble functionalites are enabled
#define GSM_ENABLE 1 // If enabled(set to > 0), all gsm functionalites are enabled
#define SD_ENABLE 0  // If enabled(set to > 0), all sd functionalites are enabled
#endif               // CONFIG_H
